# This is a SVM file .svm


# These are static memory addresses you can 
# reference during the lifetime of the program
# Commonly referred to as global variables

static: $1, $2, $3, $4, $5

# store values at static addresses
_main:

	d_push 55
	store  $1

	d_push 10
	store  $2

	# Get Address $gp + 1 = $2
	push   $gp
	push   1
	d_add

	load
	d_print








	

